@using SportStore.Domain.Entities
@model IEnumerable<Product>
@{
    ViewBag.Title = "List";
}

@foreach (var product in Model)
{
	<div>
		<h3>@product.Name</h3>
		@product.Description
		<h4>@product.Price</h4>
	</div>
}

